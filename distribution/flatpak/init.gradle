allprojects {
    buildscript {
        repositories {
            maven { url "file://${System.getenv('FLATPAK_BUILDER_BUILD_DIR')}/flatpak-gradle/res" }
        }
    }
    repositories {
        maven { url "file://${System.getenv('FLATPAK_BUILDER_BUILD_DIR')}/flatpak-gradle/res" }
    }
}
